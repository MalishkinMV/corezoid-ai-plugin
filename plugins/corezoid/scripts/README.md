# Scripts

The upstream automation scripts are bundled under `../assets/source/scripts/`. They are kept there with the rest of the source repository so relative documentation links stay easy to reason about.
